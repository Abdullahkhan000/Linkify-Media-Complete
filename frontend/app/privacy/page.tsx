import { MarketingNav } from "@/components/marketing-nav";
import { SiteFooter } from "@/components/site-footer";

export default function PrivacyPage(){return <><MarketingNav/><main className="section"><article className="container panel" style={{maxWidth:850,padding:40}}><span className="eyebrow">Privacy</span><h1 className="display" style={{fontSize:52}}>Your data, handled carefully.</h1><p>Linkify stores the minimum account and operational data needed to provide the service, protect it, and show usage analytics.</p><h2>What we store</h2><p>Account identity, hashed API credentials, billing references, support messages, and request metadata such as status and latency.</p><h2>Credential safety</h2><p>API secrets are shown once and stored only as hashes. Passwords use Django’s configured password hashers and are never stored in plaintext.</p><h2>Your choices</h2><p>You can update your profile, change your password, revoke credentials, or permanently delete your account from account settings.</p></article></main><SiteFooter/></>}
